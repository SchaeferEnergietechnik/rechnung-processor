# Pflicht
pdfplumber>=0.10.0
openpyxl>=3.1.0
requests>=2.31.0  # Für Online-OCR (OCR.space)

# Optional (für lokales OCR bei gescannten PDFs)
# pdf2image>=1.16.0
# pytesseract>=0.3.10
# (benötigt zusätzlich: Tesseract-OCR und poppler)
